Процедура ПриОткрытии()
	Если ПараметрЗапуска="autorun"  тогда
		Сообщить(ПараметрЗапуска);
			SyncOrganization();
			SyncBanks();
			SyncObjType();
			SyncObjects();
			SyncStallsType();
			SyncStalls();
			AddNewContracts();
			AddNewHalt();
		ЗавершитьРаботуСистемы(Ложь);
	КонецЕсли
КонецПроцедуры
